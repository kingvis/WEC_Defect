# Python bytecode (build artifacts)
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual env — NOT tracked (platform-specific, large, and blocks pushes through the proxy).
# Recreate with: py -3.13 -m venv .venv  (or just use py -3.13 + pip install -r requirements.txt)
.venv/
venv/
env/

# Bulk download artifact (data is already extracted into data/raw/)
wec_data.zip

# We DO track data/raw/ + data/processed/ + outputs/ in this private repo as a backup.

# OS / editor
.DS_Store
Thumbs.db
.idea/

# NDBC raw buoy text files (large; derived grid is in config/sea_states.yaml, re-fetchable)
data/ndbc/*.txt
